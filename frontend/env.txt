# Copy and rename to .env, fill out all fields

# API base URL
VITE_API_BASE_URL=/api/v1

# OAuth 2.0 paths
# Append with /providername in the code where it is used!
# Example: `${VITE_OAUTH2_AUTHORIZATION_URL}/google`
VITE_OAUTH2_AUTHORIZATION_URL=/oauth2/authorize